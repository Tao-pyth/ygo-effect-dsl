# ygo-effect-dsl

A formal Domain-Specific Language (DSL) and state-transition framework  
for modeling and exploring Yu-Gi-Oh! card effects.

---

## Overview

`ygo-effect-dsl` is a research-oriented CORE engine that:

1. Reads normalized card data (JSONL) generated by `ygo-effect-dsl-etl`
2. Converts effect text into a structured DSL
3. Validates DSL shape against spec contracts
4. Analyzes unresolved areas to guide dictionary/spec refinement
5. Provides foundations for future state-transition exploration

This repository does **not** perform data acquisition.  
All raw and normalized data is produced by the ETL repository.

---

## Source of Truth

- Spec の正は `docs/spec/` です（Single Source of Truth）。
- Wiki は導線・読み物です。

Spec v0.0:
- Grammar: `docs/spec/v0.0/10_grammar.md`
- Semantics: `docs/spec/v0.0/20_semantics.md`

---

## Documentation / Changelog Policy（運用ルール）

ドキュメント整備と変更履歴の一貫性を保つため、以下を必須ルールとします。

1. 仕様・挙動・CLI・ディレクトリ構成・運用方針を変更した場合は、関連ドキュメントを同一PRで更新する。
2. 変更を行った場合は、`docs/spec/v0.0/50_changelog.md` の `## Unreleased` に追記する。
3. **Codex（AIエージェント）による変更でも例外なく、必ずチェンジログ追記を行う。**
4. チェンジログ追記がない変更は、レビューで差し戻し対象とする。

推奨フォーマット（例）:

```md
- docs: README の運用ルールを更新（Codex変更時のChangelog追記を必須化）
```

---

## Research Objective

The goal of this project is to:

> Formalize card effects as state transition rules  
> and enable computational exploration of playable sequences.

By converting natural language effect text into structured DSL,
we make it possible to:

- Enumerate valid effect activations from a given state
- Explore opening lines (初動展開)
- Analyze reachable board states
- Compare structural properties of decks

---

## Architecture (current vertical slice)

manifest.json + cards.jsonl (from ETL export)  
        ↓  
ingest (manifest-first verification)  
        ↓  
transform (Spec v0.0 skeleton)  
        ↓  
validate (error collection)  
        ↓  
analyze (unresolved rate report)

---

## Core Concepts

### 1. DSL (Effect Formalization)

Each card effect is transformed into a structured representation:

- trigger
- condition
- cost
- action
- restriction

The DSL is deterministic and machine-processable.

### 2. State Representation

A state consists of:

- Deck
- Hand
- Field
- Graveyard
- Banished
- (optional) Stack/Chain (future extension)

States are immutable snapshots.

### 3. Transition Model

Given state S:

1. Enumerate applicable effects E
2. Apply effect e ∈ E
3. Generate next state S'
4. Repeat (bounded search)

---

## Installation

```bash
pip install -e .
```

---

## CLI Usage

### 1) ingest

Read ETL export dataset and verify `manifest.json` before `cards.jsonl`.

```bash
python -m ygo_effect_dsl ingest --dataset examples/sample_dataset
```

### 2) transform

Convert ETL export dataset to Spec v0.0 skeleton YAML.

```bash
python -m ygo_effect_dsl transform --dataset examples/sample_dataset --out data/dsl_out
```

- one file per card: `{cid}.yaml`
- no missing required keys in v0.0 skeleton
- unknown values normalized as empty values (`""`, `[]`, `{}`)

### 3) validate

Validate generated DSL against v0.0 minimum grammar constraints.

```bash
python -m ygo_effect_dsl validate data/dsl_out
```

Exit codes:
- `0`: no critical validation errors
- `1`: critical validation errors found
- `2`: argument/configuration error

### 4) analyze

Compute unresolved rates (e.g., empty object ratios) and write report.

```bash
python -m ygo_effect_dsl analyze data/dsl_out --out data/reports
```

Output:
- `data/reports/analysis_report.json`

---

## Input Contract

This repository expects ETL export artifacts:

- `manifest.json`
- `cards.jsonl`

Generated by:

- https://github.com/Tao-pyth/ygo-effect-dsl-etl

The internal SQLite database of ETL is **not** used directly; CORE depends only on export artifacts.

---

## Scope

Included:

- ingest/transform/validate/analyze pipeline
- DSL generation and contract validation
- Research experiments around formalization
- Foundations for state-transition modeling

Excluded (for now):

- API fetching
- image downloading
- direct SQLite dependency on ETL internals
- full chain/stack simulation
- opponent interaction modeling

---

## Limitations

- Current DSL transformation is skeleton-first (structure priority over deep semantics)
- No opponent interaction in v0.0 execution model
- No strict chain resolution in current core implementation

---

## Future Work

- Chain stack modeling
- Constraint solver integration
- Neo4j graph export
- Automated DSL refinement via dictionary evolution
- Performance optimization for deeper search/exploration

---

## Directory Layout (core)

- `src/ygo_effect_dsl/ingest/`
- `src/ygo_effect_dsl/transform/`
- `src/ygo_effect_dsl/validate/`
- `src/ygo_effect_dsl/analyze/`
- `src/ygo_effect_dsl/dsl/`
- `src/ygo_effect_dsl/ir/`
- `src/ygo_effect_dsl/state/` (placeholder)
- `src/ygo_effect_dsl/engine/` (placeholder)

---

## License

TBD
