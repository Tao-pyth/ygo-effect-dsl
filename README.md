# ygo-effect-dsl

A formal Domain-Specific Language (DSL) and state-transition framework  
for modeling and exploring Yu-Gi-Oh! card effects.

---

## Overview

`ygo-effect-dsl` is a research-oriented engine that:

1. Reads normalized card data (JSONL) generated by `ygo-effect-dsl-etl`
2. Converts effect text into a structured DSL
3. Models card effects as state transitions
4. Enables exploration of opening plays and board development

This repository does **not** perform data acquisition.  
All raw and normalized data is produced by the ETL repository.

---

## Research Objective

The goal of this project is to:

> Formalize card effects as state transition rules  
> and enable computational exploration of playable sequences.

By converting natural language effect text into structured DSL,
we make it possible to:

- Enumerate valid effect activations from a given state
- Explore opening lines (初動展開)
- Analyze reachable board states
- Compare structural properties of decks

---

## Architecture

cards.jsonl (from ETL)  
        ↓  
DSL Builder  
        ↓  
State Transition Engine  
        ↓  
Search / Exploration  

---

## Core Concepts

### 1. DSL (Effect Formalization)

Each card effect is transformed into a structured representation:

- trigger
- condition
- cost
- action
- restriction

The DSL is deterministic and machine-processable.

---

### 2. State Representation

A state consists of:

- Deck
- Hand
- Field
- Graveyard
- Banished
- (optional) Stack/Chain (future extension)

States are immutable snapshots.

---

### 3. Transition Model

Given state S:

1. Enumerate applicable effects E
2. Apply effect e ∈ E
3. Generate next state S'
4. Repeat (bounded search)

---

## Installation

```bash
pip install -e .
```

---

## Usage

### Load dataset

```python
from core.loader import load_cards

cards = load_cards("data/export/cards.jsonl")
```

### Build DSL

```python
from core.dsl_builder import build_dsl

dsl = build_dsl(cards[0])
```

### Run exploration

```python
from core.engine import explore

result = explore(initial_state, max_depth=5)
```

---

## Input Contract

This repository expects:

- data/export/cards.jsonl  
- data/export/manifest.json  

Generated by:

- https://github.com/Tao-pyth/ygo-effect-dsl-etl

The internal SQLite database of the ETL must not be used directly.

---

## Scope

Included:

- DSL generation
- State transition modeling
- Exploration engine
- Research experiments

Excluded:

- API fetching
- Image downloading
- Database normalization

---

## Limitations

- Initial implementation assumes:
  - No opponent interaction
  - No chain resolution (single-layer resolution)
- DSL generation may contain incomplete mappings
- Complex multi-target effects require iterative refinement

---

## Future Work

- Chain stack modeling
- Constraint solver integration
- Neo4j graph export
- Automated DSL refinement via dictionary evolution
- Performance optimization for deep search

---

## License

TBD
